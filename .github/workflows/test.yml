name: Code Quality & Testing

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  quality-checks:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: 'package-lock.json'

    - name: Install dependencies
      run: npm ci

    - name: Run code linting
      run: npm run lint

    - name: TypeScript type checking
      run: npx tsc --noEmit

    - name: Build verification
      run: npm run build

    - name: Quality checks summary
      run: |
        echo "✅ Code quality checks completed successfully"
        echo "📊 Linting, type checking, and build verification passed"
        echo "🔍 Ready for additional testing phases"

  # Future: Add Playwright E2E tests
  # e2e-tests:
  #   runs-on: ubuntu-latest
  #   needs: quality-checks
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   - name: Setup Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: ${{ env.NODE_VERSION }}
  #       cache: 'npm'
  #   - name: Install dependencies
  #     run: npm ci
  #   - name: Install Playwright browsers
  #     run: npx playwright install --with-deps
  #   - name: Run Playwright tests
  #     run: npx playwright test
  #   - name: Upload test results
  #     uses: actions/upload-artifact@v4
  #     if: always()
  #     with:
  #       name: playwright-report
  #       path: playwright-report/
  #       retention-days: 30

  # Future: Add security scanning
  # security-scan:
  #   runs-on: ubuntu-latest
  #   needs: quality-checks
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   - name: Run Trivy vulnerability scanner
  #     uses: aquasecurity/trivy-action@master
  #     with:
  #       scan-type: 'fs'
  #       scan-ref: '.'
  #       format: 'table'
  #       exit-code: '0'
  #   - name: Run npm audit
  #     run: npm audit --audit-level moderate
  #   - name: Security scan summary
  #     run: |
  #       echo "✅ Security scan completed"
  #       echo "🔒 Vulnerability assessment finished"
